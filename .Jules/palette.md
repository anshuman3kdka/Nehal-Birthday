## 2026-05-24 - [A11y Navigation Improvements]\n**Learning:** Interactive elements styled as custom navigation pills often lack default keyboard focus indicators and semantic active state indicators.\n**Action:** Ensure all custom interactive elements, such as navigation links, have `focus-visible` utility classes added to them for keyboard users, and use `aria-current='page'` for active navigation links.
